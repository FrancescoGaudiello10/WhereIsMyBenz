<div class="container">
<p id="notice"><%= notice %></p>

<h1>Listing Reviews</h1>

<div class="row">
        <div class="col-sm-4">
            <a class="btn btn-primary" href="/reviews/new">New Review </span></a>
        </div>
    </div>



  
    <% @reviews.each do |review| %>
      
      <div class="row">
            <div class="col-md-4">
                <a href="#">
                    <img class="img-responsive" src="http://placehold.it/200x150" alt="">
                </a>
            </div>
            <div class="col-md-4">
                <h3><%= review.item.name %><%= review.user.name %></h3>
                <h4><%= review.price %><%= review.store %><%= review.rating %></h4>
                <p><%= review.comment %></p>
            </div>
        </div>
        <!-- /.row -->

        <hr>

    <% end %>

<tr> <% if !@items.present? %>
  <p>There are no items matching these features.</p> </tr>
<% end %>

<%= will_paginate  @reviews %>
</div>

